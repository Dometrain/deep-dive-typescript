// Exercise 1: Imagine your app allows the user to change their data if they have permissions.
// Map over the `User` type to create a new type called `UserPermissions` with the same keys,
// but all boolean values.

type User = {
  email: string;
  age: number;
  role: string;
};

// Exercise: Create MyPartial utility that makes all properties in T optional

// Test
// type User = { id: number; name: string };
// const partialUser: MyPartial<User> = {}; // Valid, since all properties are optional

/**
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
function add(a, b) {
  return a + b;
}

add("1", 2);

// TS equivalent
// function add(a: number, b: number): number {
//   return a + b;
// }
